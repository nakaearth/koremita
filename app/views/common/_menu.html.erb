<p class="navbar-text pull-right">
<% if current_user %>
  Logged in as <strong><%= current_user.email %></strong>.
  <%= image_tag src: "#{current_user.auth_providers[0].image_path}", size: '40x40' %>
  <%= link_to "ログアウト", logout_path  %>
<% else %>
  <%= link_to "新規登録", "/facebook/login"  %> |
<% end %>
